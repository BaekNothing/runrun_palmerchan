using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "FocusDataObjects", menuName = "ScriptableObjects/FocusDataObjects", order = 1)]
public class FocusDataObjects : ScriptableObject
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
